#ifndef ENUM_HPP_
    #define ENUM_HPP_

enum screens {
    mainMenu,
    settingsFirstMenu,
    settingsSecondMenu,
    leaderboardMenu,
    helpMenu,
    pauseMenu,
    deadMenu,
    playScreen
};

enum buttonState {
    isNone,
    mouseHover,
    mouseClicked,
    isDisabled
};

#endif /* !ENUM_HPP_ */
