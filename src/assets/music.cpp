#include "music.hpp"
#include "all.hpp"
#include <iostream>

Music::Music(UN const std::string &filepath)
{
/*     if (not music.openFromFile(filepath)) {
        std::cerr << "Problem with " + filepath + "." << std::endl;
        exit(84);
    } */
}
